# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=schedule_db

# Note: If these database settings are not configured or connection fails,
# the server will automatically fallback to a file-based storage (schedules.json) 
# so the application runs immediately without requiring MySQL setup.
